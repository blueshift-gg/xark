[package]
name = "aes"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
xark = { path = "../../crates/xark", default-features = false }
xark-bits = { path = "../../crates/xark-bits" }
xark-aes = { path = "../../crates/xark-aes" }
