[package]
name = "aes256_gcm"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
xark = { path = "../../crates/lang", version = "0.2.2" }
xark-aes = { path = "../../gadgets/xark-aes", version = "0.2.2" }

[dev-dependencies]
# Reference AES-GCM (renamed to avoid colliding with this crate's own name).
aesgcm-ref = { package = "aes-gcm", version = "0.10" }

[workspace]
