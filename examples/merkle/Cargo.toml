[package]
name = "merkle"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
xark = { path = "../../crates/xark", default-features = false }
xark-merkle = { path = "../../crates/xark-merkle" }

[workspace]
