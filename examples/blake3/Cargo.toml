[package]
name = "blake3"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
xark = { path = "../../crates/lang", version = "0.2.2" }
xark-blake3 = { path = "../../gadgets/xark-blake3", version = "0.2.2" }

[dev-dependencies]
# Reference BLAKE3 (renamed to avoid colliding with this crate's own name).
blake3-ref = { package = "blake3", version = "1" }

[workspace]
