# Regenerated by `regenerate.sh` from the committed verifying-key fixture;
# do not commit. The Rust test in `crates/groth16-backend/tests/evm_export.rs`
# pins the SHA-256 so the on-disk artifact stays reproducible.
Verifier.sol

# Foundry build artifacts.
out/
cache/
broken_lib/
lib/
