# Minimal Foundry config for the WS-E.1 smoke test.
# See `README.md` in this directory for how to run.

[profile.default]
src = "."
test = "."
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
