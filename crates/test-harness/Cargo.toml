[package]
name = "xark-test-harness"
description = "Test harness for xark circuits: compile-and-solve helpers shared across the gadget test suites."
keywords = ["zero-knowledge", "zk", "cryptography", "circuit", "groth16"]
categories = ["cryptography"]
license.workspace = true
readme = "README.md"
version.workspace = true
edition = "2024"
[dependencies]
num-bigint = "0.4"
xark-ir = { path = "../ir", version = "0.2.1" }
