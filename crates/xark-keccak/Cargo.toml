[package]
name = "xark-keccak"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
name = "xark_keccak"
path = "src/lib.rs"

[dependencies]
xark = { path = "../xark", default-features = false }
xark-bits = { path = "../xark-bits" }
