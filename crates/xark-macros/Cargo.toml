[package]
name = "xark-macros"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "Proc macros for xark circuits (the `#[circuit]` attribute)."

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
